# colcon workspace (populated via scripts/setup.sh, not committed)
/src/
/build/
/install/
/log/

# internal planning notes, not part of the published project
# (PROGRESS.md and ARCHITECTURE.md are tracked deliverables, not excluded)
PLAN.md

# Python bytecode from running scripts/ directly
__pycache__/
*.pyc
